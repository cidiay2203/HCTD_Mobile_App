import {saveToken, getToken, removeToken} from './token';
import {saveProfile, getProfile, removeProfile} from './profile';

export {
    saveToken, 
    getToken,
    removeToken,
    saveProfile,
    getProfile,
    removeProfile
}
